# Project Status (v1.0.1)

This page exists so users can clearly distinguish the legacy `1.0.1` line from the staged-major `2.0.0` line.

Use `1.0.1` only if you need the older public API and migration context.

For new work, use the latest documentation and release line.
