# Installation (v1.0.1)

Install the legacy release line explicitly:

```bash
pip install "proofofthought==1.0.1"
```

The package import path remains:

```python
from z3adapter.reasoning import ProofOfThought
```

For the staged-major release, return to the latest docs:

<https://debarghaG.github.io/proofofthought/>
